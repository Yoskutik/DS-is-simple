import React from 'react';
import './style.scss';

const Header = (): React.ReactElement => (
    <header className="app__header">
        <h1 className="app__header_title">
            Data Science - это просто!
        </h1>
    </header>
);

export default Header;
